#include <stdio.h>

int	ft_fibonacci(int index);

int main()
{
    // int i = 0;

    // while (i < 20)
    // {
    // printf("The val %d is %d\n",i,ft_fibonacci(i));
    // i++;
    // }

    printf("The val is %d\n",ft_fibonacci(20));
}